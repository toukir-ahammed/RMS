@model IEnumerable<RMSDataModel.Enrollment>

@{
    ViewBag.Title = "StudentList";
}

<h2>StudentList</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.Name)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", "Enrollments", new { id=item.EnrollmentId }) |
            @Html.ActionLink("Details", "Details", "Enrollments", new { id=item.EnrollmentId }) |
            @Html.ActionLink("Delete", "Delete", "Enrollments"new { id=item.EnrollmentId })
        </td>*@
    </tr>
}

</table>
